folder `lidar` is my code, folder `lidar/rplidarc1` is https://github.com/dsaadatmandi/rplidarc1/tree/main/rplidarc1

`_lidar.config` should be a plaintext, one-line file with the port of the LiDAR. It defaults to `/dev/ttyUSB0`, but on Windows it could be something like `COM4`.
